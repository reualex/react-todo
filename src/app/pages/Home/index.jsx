import React from "react";
import strings from "../../../../localization";

const Home = () => {
  return (
    <div>
        <div>{strings.links.home}</div>

    </div>
  );
};

export default Home;
